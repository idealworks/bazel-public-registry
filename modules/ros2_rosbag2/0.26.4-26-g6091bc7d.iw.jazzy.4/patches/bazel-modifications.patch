diff --git a/BUILD.bazel b/BUILD.bazel
index 7fae405..e544474 100644
--- a/BUILD.bazel
+++ b/BUILD.bazel
@@ -408,6 +408,22 @@ py_library(
     deps = ["@ros2_rpyutils//:rpyutils"],
 )
 
+py_library(
+    name = "ros2bag_mcap_cli",
+    srcs = ["rosbag2_storage_mcap/ros2bag_mcap_cli/__init__.py"],
+    imports = ["rosbag2_storage_mcap"],
+    data = ["rosbag2_storage_mcap/ros2bag_mcap_cli.dist-info/entry_points.txt"],
+    visibility = ["//visibility:public"],
+)
+
+py_library(
+    name = "ros2bag_sqlite3_cli",
+    srcs = ["rosbag2_storage_sqlite3/ros2bag_sqlite3_cli/__init__.py"],
+    imports = ["rosbag2_storage_sqlite3"],
+    data = ["rosbag2_storage_sqlite3/ros2bag_sqlite3_cli.dist-info/entry_points.txt"],
+    visibility = ["//visibility:public"],
+)
+
 py_library(
     name = "ros2bag",
     srcs = glob(["ros2bag/ros2bag/**/*.py"]),
@@ -415,6 +431,8 @@ py_library(
     visibility = ["//visibility:public"],
     deps = [
         ":rosbag2_py",
+        ":ros2bag_mcap_cli",
+        ":ros2bag_sqlite3_cli",
         "@ros2cli",
         requirement("pyyaml"),
     ],
diff --git a/MODULE.bazel b/MODULE.bazel
index 2cad983..f52b975 100644
--- a/MODULE.bazel
+++ b/MODULE.bazel
@@ -1,6 +1,6 @@
 module(
     name = "ros2_rosbag2",
-    version = "0.26.4-26-g6091bc7d.iw.jazzy.3",
+    version = "0.26.4-26-g6091bc7d.iw.jazzy.4",
     compatibility_level = 1,
 )
 
diff --git a/ros2bag/ros2bag/api/__init__.py b/ros2bag/ros2bag/api/__init__.py
index 42af8c9..626a92f 100644
--- a/ros2bag/ros2bag/api/__init__.py
+++ b/ros2bag/ros2bag/api/__init__.py
@@ -300,7 +300,7 @@ def add_writer_storage_plugin_extensions(parser: ArgumentParser) -> None:
             [('none', 'Default writer configuration.')]
     except AttributeError:
         print(f'Storage plugin {plugin_id} does not provide function "get_preset_profiles".')
-        preset_profiles = ['none']
+        preset_profiles = [('none', 'Default writer configuration.')]
     default_preset_profile = preset_profiles[0][0]
     parser.add_argument(
         '--storage-preset-profile', type=str, default=default_preset_profile,
diff --git a/rosbag2_cpp/src/rosbag2_cpp/cache/circular_message_cache.cpp b/rosbag2_cpp/src/rosbag2_cpp/cache/circular_message_cache.cpp
index d26d04e..3d89494 100644
--- a/rosbag2_cpp/src/rosbag2_cpp/cache/circular_message_cache.cpp
+++ b/rosbag2_cpp/src/rosbag2_cpp/cache/circular_message_cache.cpp
@@ -47,12 +47,18 @@ void CircularMessageCache::push(std::shared_ptr<const rosbag2_storage::Serialize
 }
 
 std::shared_ptr<CacheBufferInterface> CircularMessageCache::get_consumer_buffer()
+#if defined(__clang__)
+__attribute__((no_thread_safety_analysis))
+#endif
 {
   consumer_buffer_mutex_.lock();
   return consumer_buffer_;
 }
 
 void CircularMessageCache::release_consumer_buffer()
+#if defined(__clang__)
+__attribute__((no_thread_safety_analysis))
+#endif
 {
   consumer_buffer_mutex_.unlock();
 }
diff --git a/rosbag2_cpp/src/rosbag2_cpp/cache/message_cache.cpp b/rosbag2_cpp/src/rosbag2_cpp/cache/message_cache.cpp
index 139cb6f..76ace72 100644
--- a/rosbag2_cpp/src/rosbag2_cpp/cache/message_cache.cpp
+++ b/rosbag2_cpp/src/rosbag2_cpp/cache/message_cache.cpp
@@ -61,12 +61,18 @@ void MessageCache::push(std::shared_ptr<const rosbag2_storage::SerializedBagMess
 }
 
 std::shared_ptr<CacheBufferInterface> MessageCache::get_consumer_buffer()
+#if defined(__clang__)
+__attribute__((no_thread_safety_analysis))
+#endif
 {
   consumer_buffer_mutex_.lock();
   return consumer_buffer_;
 }
 
 void MessageCache::release_consumer_buffer()
+#if defined(__clang__)
+__attribute__((no_thread_safety_analysis))
+#endif
 {
   consumer_buffer_mutex_.unlock();
 }
diff --git a/rosbag2_cpp/src/rosbag2_cpp/serialization_format_converter_factory_impl.hpp b/rosbag2_cpp/src/rosbag2_cpp/serialization_format_converter_factory_impl.hpp
index b338a15..149957a 100644
--- a/rosbag2_cpp/src/rosbag2_cpp/serialization_format_converter_factory_impl.hpp
+++ b/rosbag2_cpp/src/rosbag2_cpp/serialization_format_converter_factory_impl.hpp
@@ -57,8 +57,7 @@ public:
         "rosbag2_cpp",
         converter_interfaces::SerializationFormatDeserializer::get_base_class_name());
     } catch (const std::exception & e) {
-      ROSBAG2_CPP_LOG_ERROR_STREAM("Unable to create class loader instance: " << e.what());
-      throw e;
+      ROSBAG2_CPP_LOG_DEBUG_STREAM("Unable to create class loader instance: " << e.what());
     }
   }
 
@@ -67,17 +66,26 @@ public:
   std::unique_ptr<converter_interfaces::SerializationFormatDeserializer>
   load_deserializer(const std::string & format)
   {
+    if (deserializer_class_loader_ == nullptr) {
+      return nullptr;
+    }
     return load_interface(format, deserializer_class_loader_);
   }
 
   std::unique_ptr<converter_interfaces::SerializationFormatSerializer>
   load_serializer(const std::string & format)
   {
+    if (serializer_class_loader_ == nullptr) {
+      return nullptr;
+    }
     return load_interface(format, serializer_class_loader_);
   }
 
   std::vector<std::string> get_declared_serialization_plugins() const
   {
+    if (serializer_class_loader_ == nullptr) {
+      return {};
+    }
     return serializer_class_loader_->getDeclaredClasses();
   }
 
diff --git a/rosbag2_py/src/rosbag2_py/_writer.cpp b/rosbag2_py/src/rosbag2_py/_writer.cpp
index bdac2c1..04242c9 100644
--- a/rosbag2_py/src/rosbag2_py/_writer.cpp
+++ b/rosbag2_py/src/rosbag2_py/_writer.cpp
@@ -83,12 +83,16 @@ std::unordered_set<std::string> get_registered_compressors()
 
 std::unordered_set<std::string> get_registered_serializers()
 {
-  auto serializers = rosbag2_cpp::plugins::get_class_plugins<
-    rosbag2_cpp::converter_interfaces::SerializationFormatSerializer>();
-  auto converters = rosbag2_cpp::plugins::get_class_plugins<
-    rosbag2_cpp::converter_interfaces::SerializationFormatConverter>();
-  serializers.insert(converters.begin(), converters.end());
-  return serializers;
+  try {
+    auto serializers = rosbag2_cpp::plugins::get_class_plugins<
+      rosbag2_cpp::converter_interfaces::SerializationFormatSerializer>();
+    auto converters = rosbag2_cpp::plugins::get_class_plugins<
+      rosbag2_cpp::converter_interfaces::SerializationFormatConverter>();
+    serializers.insert(converters.begin(), converters.end());
+    return serializers;
+  } catch (const std::exception&) {
+    return {};
+  }
 }
 
 }  // namespace rosbag2_py
diff --git a/rosbag2_transport/src/rosbag2_transport/player.cpp b/rosbag2_transport/src/rosbag2_transport/player.cpp
index 522d04c..528a73e 100644
--- a/rosbag2_transport/src/rosbag2_transport/player.cpp
+++ b/rosbag2_transport/src/rosbag2_transport/player.cpp
@@ -323,6 +323,18 @@ private:
     int(
       const rosbag2_storage::SerializedBagMessageSharedPtr &,
       const rosbag2_storage::SerializedBagMessageSharedPtr &)>;
+  /// Comparator for SerializedBagMessageSharedPtr to order chronologically by recv_timestamp.
+  // NOTE: Declared before message_queue_ so it is initialized first, since the constructor
+  // passes it to message_queue_'s constructor (member init follows declaration order).
+  struct
+  {
+    bool operator()(
+      const rosbag2_storage::SerializedBagMessageSharedPtr & l,
+      const rosbag2_storage::SerializedBagMessageSharedPtr & r) const
+    {
+      return l->recv_timestamp > r->recv_timestamp;
+    }
+  } bag_message_chronological_recv_timestamp_comparator;
   LockedPriorityQueue<
     rosbag2_storage::SerializedBagMessageSharedPtr,
     std::vector<rosbag2_storage::SerializedBagMessageSharedPtr>,
@@ -371,17 +383,6 @@ private:
   std::vector<KeyboardHandler::callback_handle_t> keyboard_callbacks_;
 
   std::shared_ptr<PlayerServiceClientManager> player_service_client_manager_;
-
-  /// Comparator for SerializedBagMessageSharedPtr to order chronologically by recv_timestamp.
-  struct
-  {
-    bool operator()(
-      const rosbag2_storage::SerializedBagMessageSharedPtr & l,
-      const rosbag2_storage::SerializedBagMessageSharedPtr & r) const
-    {
-      return l->recv_timestamp > r->recv_timestamp;
-    }
-  } bag_message_chronological_recv_timestamp_comparator;
 };
 
 PlayerImpl::PlayerImpl(
